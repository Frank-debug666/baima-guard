# Contributing

Keep contributions privacy-safe and test both risk and normal-message contexts.

Before submitting changes:

1. Do not commit real private SMS messages, phone numbers, email addresses, credentials, or personal images.
2. Add regression samples for every new risk pattern.
3. Add a normal safety-context sample when a pattern may cause false positives.
4. Run `run_tests.bat` and build the frontend.
